// Package smoketest will hold Go-based integration tests for the
// composite action defined in action.yml: installing the pinned tapelock
// binary, running `tapelock check`, and asserting the workflow's exit
// code. Not implemented yet — see CONTRIBUTING.md for the v1 scope.
package smoketest
